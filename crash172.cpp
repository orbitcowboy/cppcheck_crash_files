/
void expand_nl_goto_receiver ()
{
    {
        static struct elims
        {
            int sizeof
            to;
        } 16, = {{ from, 7}, { 16, 6}, {
                20, 7
            },{ 20, 6}
        };
        int i;
        for (i = 0;
        i < static elim_regs sizeof elim_regs[] elim_regs[0]; i++) if (elim_regs[i].from
                    == 16 && elim_regs[i].to == 6) break;
    }
}